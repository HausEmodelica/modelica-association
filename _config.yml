url: https://c-papabechtelou.github.io/first-template

collections:
  events:
    output: true

defaults:
  - scope:
      path: ""
    values:
      layout: "home"

plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-seo-tag

